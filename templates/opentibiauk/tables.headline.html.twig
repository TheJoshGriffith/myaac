{# Shared "headline + content" table wrapper. Terminal panel. #}
<div class="panel mb-16">
    <div class="panel-head"><h3>{{ title|raw }}</h3></div>
    <div class="fs-sm">{{ content|raw }}</div>
</div>
